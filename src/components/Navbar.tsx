"use client";

import Image from "next/image";

const Navbar = () => {
  return (
    <nav className="bg-red-500 xl:max-w-[120rem] w-full">
      {/* <Image sr /> */}
    </nav>
  );
};

export default Navbar;
